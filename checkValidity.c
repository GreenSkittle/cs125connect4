// write a function that receives a pointer to the board, a location.
// if the player is able to go in that location (0-6) return a 1
// if the location is invalid, such as -1,
// or the column is filled to the top, return a zero!
int checkValidity(int* pointy, column){

return result;
}
